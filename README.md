# Autism Spectrum Disorder (ASD) Screening Web Application

A full-stack web application for **Autism Spectrum Disorder (ASD) screening using facial images**. The application uses a Vision Transformer (ViT) model for inference and provides predictions through a React web interface.

## Tech Stack

- **Backend:** FastAPI (Python)
- **Frontend:** React + Vite + TypeScript
- **Deep Learning:** Vision Transformer (ViT)
- **Frameworks:** PyTorch, Hugging Face Transformers

---

# Prerequisites

Install the following before starting:

- Git
- Python 3.10 or later
- Node.js (includes npm)

Verify installation:

```bash
git --version
python --version
node --version
npm --version
```

---

# Clone the Repository

```bash
git clone https://github.com/aniruddramesh/Stuttering_classifier.git
cd Stuttering_classifier
```

---

# Project Structure

```
Stuttering_classifier
│
├── backend
│   ├── app
│   ├── inference
│   ├── models
│   ├── requirements.txt
│   └── ...
│
├── frontend
│   ├── src
│   ├── package.json
│   └── ...
│
├── training
│
├── dataset
│
└── README.md
```

---

# Model

The trained Vision Transformer checkpoint is **not included** in this repository.

Place the trained model here:

```
training/checkpoints/best_model.pth
```

---

# Backend Setup

Open a terminal:

```bash
cd backend
```

Create a virtual environment:

```bash
python -m venv venv
```

Activate it

### Windows

```bash
venv\Scripts\activate
```

### Linux/macOS

```bash
source venv/bin/activate
```

Install dependencies

```bash
pip install -r requirements.txt
```

Run the backend

```bash
python -m uvicorn app.main:app --reload
```

Backend will start at

```
http://127.0.0.1:8000
```

Swagger API documentation:

```
http://127.0.0.1:8000/docs
```

---

# Frontend Setup

Open another terminal.

```bash
cd frontend
```

Install packages

```bash
npm install
```

Start the development server

```bash
npm run dev
```

Frontend runs at

```
http://localhost:5173
```

---

# Using the Application

1. Open the frontend.
2. Click **Open Camera**.
3. Capture a facial image.
4. Wait for the prediction.
5. View:
   - Prediction
   - Confidence score
   - Class probabilities
   - Disclaimer

---

# Building the Frontend

To create a production build:

```bash
cd frontend
npm run dev
```

The production files will be generated in:

```
frontend/dist
```

These can be served through the FastAPI backend for deployment.

---

# API Endpoint

## POST `/predict`

Accepts a facial image.

Supported formats:

- JPG
- JPEG
- PNG

Example using curl:

```bash
curl -X POST ^
-F "image=@face.jpg" ^
http://127.0.0.1:8000/predict
```

Example response:

```json
{
    "prediction": "ASD",
    "confidence": 97.89,
    "probabilities": {
        "ASD": 97.89,
        "Control": 2.11
    },
    "disclaimer": "This result is generated by a research prototype and is not a medical diagnosis."
}
```

---

# Troubleshooting

## Python not found

Install Python and ensure it is added to your system PATH.

---

## npm not found

Install node.js.

---

## Model not found

Ensure the model exists at

```
training/checkpoints/best_model.pth
```

---

## Camera not opening

- Allow camera permission in your browser.
- Ensure no other application is using the webcam.
- Refresh the page.

---

## Port already in use

Backend

```bash
python -m uvicorn app.main:app --reload
```

Frontend

```bash
npm run dev
```

---

# Disclaimer

This application is intended **for research and educational purposes only**.

It **does not provide a medical diagnosis** and should not replace evaluation by qualified healthcare professionals.

---

# License

This project is released under the MIT License.

---

# Author

**Bhuvan R**

GitHub:
https://github.com/Thor-asgardian/Stuttering_classifier